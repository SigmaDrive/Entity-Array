import java.util.Random;

public class Field
{
  //execution method
  public static void  main(String[] args)
  {
    Random generator = new Random();
    int localSeed = 0;
    EntSlot[] entArray = new EntSlot[100];
    for (int x = 0; x < entArray.length; x ++)
    {
      localSeed = generator.nextInt(1000);
      
    }
  }
}
